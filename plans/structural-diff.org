* Structural Diff Feature

Detect which functions, methods, classes, and structs were added/modified/deleted
by combining tree-sitter structure extraction with diff line information.

** Goals

*** Phase 1: Basic structural diff [COMPLETED]
- [X] Extract structure from both old and new file contents
- [X] Match elements by name between old and new
- [X] Determine change status based on diff line overlap
- [X] Store structural diff in model for display
- [X] Display structural diff in view (below file header, above hunks)
- [X] Cursor on structural diff rows shows file in top bar (no line number)
- [X] Tree structure for nested elements (methods under types)

*** Phase 2: Rename/move detection (future)
- Detect when a function is renamed but content is identical
- Detect when a function is moved (same name, same content, different lines)
- Use content hashing for efficient comparison
- Special representation for renames vs modifications

** Requirements (from discussion)

#+begin_quote
"first use-case is to list the parts that changed for easy/quick overview.
later use-case could include filtering, or listing all the key parts of file
for navigation. but initially I'm just thinking about diffing"
#+end_quote

#+begin_quote
"functions/methods/classes, and any key composite objects -- so structs in Go
could be one"
#+end_quote

#+begin_quote
"I think we want to keep it simple meaning we get the boundaries of that thing
and detect if there were any diff changes within those boundaries"
#+end_quote

#+begin_quote
"separate items, and then when we display them it will probably be as some kind
of tree where we say '~ class X' and then under that '+ my_new_method' or
'~ my_changed_method', or nothing because the part that changed was not
belonging to a class or method"
#+end_quote

#+begin_quote
"it depends a lot on performance. I think ideally we would compute all this
within the same async 'loading' we do for a file -- so when we compute the
syntax and so forth"
#+end_quote

** Existing Infrastructure

*** pkg/structure/ package
- ~Entry~ struct with: StartLine, EndLine, Name, Kind, Receiver, Params, ReturnType
- ~Map~ struct with ~AtLine(line int) []Entry~ for breadcrumb lookup
- ~Extractor~ dispatches to language-specific extractors
- Go extractor handles: function_declaration, method_declaration, type_declaration

*** pkg/highlight/highlighter.go
- ~HighlightWithStructure()~ parses once and returns both spans and structure
- Currently only called for NEW content (old uses plain ~Highlight()~)

*** internal/tui/highlight.go
- ~RequestHighlight()~ triggers async highlighting
- Stores structure in ~m.structureMaps[fileIndex].NewStructure~
- Only extracts structure from new content currently

** Design

*** New types (pkg/structure/diff.go or similar)

#+begin_src go
type ChangeKind int

const (
    ChangeUnchanged ChangeKind = iota
    ChangeAdded
    ChangeDeleted
    ChangeModified
    // Future: ChangeMoved, ChangeRenamed
)

type ElementChange struct {
    Kind       ChangeKind
    OldEntry   *Entry  // nil for Added
    NewEntry   *Entry  // nil for Deleted
}

type StructuralDiff struct {
    Changes []ElementChange
}
#+end_src

*** Algorithm

1. Extract structure from old content -> oldMap
2. Extract structure from new content -> newMap
3. Build name->entry maps for both
4. For each entry in newMap:
   - If name exists in oldMap: matched (check if modified)
   - Else: added
5. For each entry in oldMap not matched: deleted
6. For matched pairs, check if any diff lines overlap with their boundaries:
   - Get changed line numbers from FilePair.Pairs (lines with Type == Added or Removed)
   - If old entry's range overlaps with removed lines OR new entry's range overlaps
     with added lines -> modified
   - Else -> unchanged

*** Integration points

**** highlight.go changes
- Also call ~HighlightWithStructure()~ for old content (not just new)
- Store both ~OldStructure~ and ~NewStructure~ in FileStructure
- Compute structural diff after both are available

**** model.go changes
- Extend ~FileStructure~ to include ~OldStructure *structure.Map~
- Add ~StructuralDiff *structure.StructuralDiff~ field

**** commands.go changes
- Structural diff computation happens in the same async flow as highlighting
- No additional async work needed

** Performance Considerations

- Tree-sitter parsing already happens for highlighting
- Structure extraction reuses the same parse tree (HighlightWithStructure)
- Matching is O(n) with hash map lookups
- Diff line overlap check is O(entries * hunks) but both are typically small
- Total overhead should be minimal

** Languages

Starting with Go only. Python extractor exists but not prioritized.

Future: extend to other languages by adding extractors.

** Testing Strategy

- Unit tests for diff algorithm (given old entries, new entries, changed lines -> expected changes)
- Integration tests with real Go files
- Golden file tests for structural diff output format

** Open Questions

- How to represent nested structures in the diff output?
  - Decision: flat list of all elements, display logic handles tree rendering
- Should we diff at the content level (actual code changes) or just line overlap?
  - Decision: line overlap is simpler and sufficient for v1

** Implementation Notes (Phase 1)

*** Files changed
- ~pkg/structure/diff.go~ - Core diff types and ComputeDiff algorithm
- ~pkg/structure/diff_test.go~ - Unit tests for diff logic
- ~internal/tui/messages.go~ - Added OldStructure to HighlightReadyMsg
- ~internal/tui/model.go~ - Added OldStructure and StructuralDiff to FileStructure
- ~internal/tui/highlight.go~ - Extract old structure, compute diff, extractChangedLines helper
- ~internal/tui/view.go~ - RowKindStructuralDiff, buildStructuralDiffRows, renderStructuralDiffRow
- ~internal/tui/highlight_test.go~ - Integration tests for structural diff

*** Display format
Structural diff appears between file header and first hunk:
#+begin_example
  ~ func FuncA
  + func NewFunc
  - func DeletedFunc
  ~ type MyStruct
    ~ func (m MyStruct) ModifiedMethod
#+end_example

Methods within types are indented. Symbols: + (added), - (deleted), ~ (modified).

*** Cursor behavior
When cursor is on structural diff rows:
- Top bar shows file info (same as being on file header)
- No line number or breadcrumbs shown
- fileIndex is set to the actual file (unlike commit body which uses -1)

** Phase 1.5: Move structural diff into file header [IN PROGRESS]

Move structural diff from being content below the header to being part of the
header itself - visible even in folded mode.

*** Requirements (from discussion)

#+begin_quote
"I'd like to try making it part of the file header itself -- even the folded
view. Essentially making the folded version of the header occupy multiple lines.
when the header enters its border/expand mode, that syntax content would be
included within the border contents -- so the border essentially gets drawn
around the whole lot. but the blue shader line would continue to only affect
the same line as the filename"
#+end_quote

#+begin_quote
"DONT include the left border -- the current design only has borders on the
other three sides. You may need to increase the width of the bordered area if
the code structure items are wider than the file line. You should enforce some
sensible limit (eg. anything more than 80% of the screen width gets truncated)"
#+end_quote

#+begin_quote
"let's have the first icon (+/-/~) align with the same icon on the filename --
so we start two characters before the filename starts"
#+end_quote

*** Visual design

**** FoldFolded (no borders):
#+begin_example
     ◐ #01 ~ internal/tui/view.go  +3 -1   │▒▒▒▒
           ~ func BuildRows
           + func NewHelper
#+end_example

**** FoldNormal/FoldExpanded (with borders):
#+begin_example
────────────────────────────────────────────┐
     ◐ #01 ~ internal/tui/view.go  +3 -1   │▒▒▒▒
           ~ func BuildRows                 │
           + func NewHelper                 │
────────────────────────────────────────────┘
#+end_example

Key visual rules:
- Structural diff symbol (+/-/~) aligns with file status symbol
- Blue shading (▒) only on filename line, not structural diff lines
- Box width expands if structural diff lines are wider (max 80% screen width)
- Each structural diff line remains a separate cursor target
- No structural diff = single-line header as before

*** Implementation plan

**** Step 1: Calculate box width considering structural diff [DONE]
In ~buildAllRows~:
- [X] Add helper function to compute max structural diff content width for a file
- [X] After computing ~headerBoxWidth~, also consider max structural diff line width
- [X] Take ~max(headerBoxWidth, maxStructuralDiffWidth)~
- [X] Clamp to 80% of ~m.width~ (only when width would exceed limit)

**** Step 2: Modify row ordering in buildFileRows [DONE]
- [X] FoldFolded: header row -> structural diff rows (no borders)
- [X] FoldNormal/FoldExpanded: top border -> header -> structural diff -> bottom border -> content
- [X] Pass ~headerBoxWidth~ and ~borderVisible~ to structural diff rows
- [X] Remove trailing blank separator (bottom border serves this purpose)

**** Step 3: Update buildStructuralDiffRows formatting [DONE]
- [X] Calculate numDigits from total file count inside function
- [X] Calculate proper indentation: ~9 + numDigits~ chars before symbol
- [X] Child items get additional 2-space indent before symbol
- [X] Set ~headerBoxWidth~ and ~borderVisible~ on each row (done in step 2)

**** Step 4: Add fields to displayRow struct [DONE]
- [X] ~headerBoxWidth~ field already exists, reused for structural diff
- [X] ~borderVisible~ field already exists, reused for structural diff

**** Step 5: Rewrite renderStructuralDiffRow [DONE]
- [X] Parse content to extract prefix, symbol, and rest
- [X] Style symbol based on change kind (+/-/~)
- [X] Add box padding to reach headerBoxWidth
- [X] Add │ border at right edge (no trailing fill ▒)
- [X] Handle cursor: replace first 5 chars of prefix with cursor elements
- [X] Border uses darker style when previous file is folded

**** Step 6: Handle nested items (methods under types) [DONE]
- [X] Child items already have 2-char extra indent (handled in step 3)
- [X] Same border handling as parent items (same renderStructuralDiffRow)

*** Files to modify
- ~internal/tui/view.go~ - buildAllRows, buildFileRows, buildStructuralDiffRows, renderStructuralDiffRow

*** Follow-up improvements

**** Bug: Border alignment
#+begin_quote
"bug with border display -- items are not indented enough to align with the
right border"
#+end_quote
- [ ] Fix structural diff line width calculation to properly align with │ border

**** Truncate folded view to 10 items
#+begin_quote
"let's truncate to 10 items max for fold view. any more than that we show '...'
as a line. when the content is unfolded, we'll show the wider list. this means
the node header can essentially have the folded extra representation and then
even more for the border representation"
#+end_quote
- [ ] In FoldFolded mode, limit to 10 structural diff items
- [ ] Show "..." line when truncated
- [ ] In FoldNormal/FoldExpanded, show full list

**** Update colours [DONE]
#+begin_quote
"let's change the colours: the func/type/class part becomes colour 8 and the
identifier part becomes colour 7"
#+end_quote
- [X] Style "func"/"type"/"class" keyword with fg=8
- [X] Style identifier name with fg=7

**** Add indentation [DONE]
#+begin_quote
"let's add two spaces indentation for our list to start under the filename --
so two more indented than the symbol on the filename"
#+end_quote
- [X] Add 2 more spaces to symbolPrefix (11 + numDigits instead of 9 + numDigits)
- [X] Update symbolPos in renderStructuralDiffRow accordingly

**** Explore blank line before last folded item
#+begin_quote
"when we have this preview in folded mode available, let's MAYBE add a blank
line before the last folded item -- to explore that"
#+end_quote
- [ ] Experiment with adding blank line separator in folded mode

**** Per-element diff stats
#+begin_quote
"the diff columns in the fileline could be shoved over to take account for the
length of the code item name, and then the code items could have their own +/-
values but in grey, so we can see more easily if the changes are concentrated
in a particular function or not"
#+end_quote
#+begin_quote
"we could also SORT the items so we just show the top N (say 5) by their change
count"
#+end_quote
- [ ] Calculate +/- line counts per structural element (using line ranges)
- [ ] Display grey +N -M after each element name
- [ ] Adjust file header diff column position to align with longest element line
- [ ] Sort elements by total change count (added + removed lines)
- [ ] Show only top N (e.g. 5) most-changed elements in folded view
